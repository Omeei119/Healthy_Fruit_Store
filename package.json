{
  "name": "fruit-store-backend",
  "version": "1.0.0",
  "description": "JSON Server for Fruit Store Backend",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch data_for_json_server.json --port 10000"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
