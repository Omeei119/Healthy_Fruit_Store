{
  "name": "fruit-store-backend",
  "version": "1.0.0",
  "description": "JSON Server for Fruit Store Backend",
  "main": "index.js",
  "scripts": {
    "start": "json-server db.json --port 10000 --host 0.0.0.0 --middlewares cors.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "json-server": "^0.17.3"
  }
}
